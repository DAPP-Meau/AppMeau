export type Person = {
  fullName: string;
  age: number;
};

export type Address = {
  state: string;
  city: string;
  address: string;
  phone: string;
}

export type Login = {
  email: string;
  password: string;
}