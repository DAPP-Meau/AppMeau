import { FirebaseApp } from "firebase/app"
import { PetRegistrationDocument } from "../models"

export async function listPetAction(
  firebaseApp: FirebaseApp,
): Promise<Array<PetRegistrationDocument>> {
  // db = getDatabe(firebaseApp)
  //
}
